$ ->
    $("input.blind").click ->
        $("p.blind").toggle "blind"
        return

    $("input.clip").click ->
        $("p.clip").toggle "clip"
        return

    $("input.drop").click ->
        $("p.drop").toggle "drop"
        return

    $("input.explode").click ->
        $("p.explode").toggle "explode"
        return

    $("input.fold").click ->
        $("p.fold").toggle "fold"
        return

    $("input.puff").click ->
        $("p.puff").toggle "puff"
        return

    $("input.pulsate").click ->
        $("p.pulsate").toggle "pulsate"
        return

    $("input.scale").click ->
        $("p.scale").toggle "scale"
        return

    $("input.slide").click ->
        $("p.slide").toggle "slide"
        return

    $("input.bounce").click ->
        $("p.bounce").toggle "bounce"
        return

    $("input.shake").click ->
        $("p.shake").toggle "shake"
        return

    $("input.highlight").click ->
        $("p.highlight").toggle "highlight"
        return

    $("input.size").click ->
        $("p.size").toggle "size"
        return

    return
