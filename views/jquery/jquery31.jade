extends ../layout

block link
	include ../link1

block content
	include navbar
	.container
		.row
			.col-sm-3
				include left
			.col-sm-9
				.panel.panel-primary
					.panel-heading
						i.glyphicon.glyphicon-book
						| &nbsp;removeClass()メソッド
					.panel-body
						p removeClass()メソッドは、$メソッドで指定した要素に適用されたクラスのうち、addClassメソッドの引数で指定したクラスを外します。
						p
							a.btn.btn-default(href='../demo/jquery/exercise28/index1.html',target='_blank')
								i.glyphicon.glyphicon-play
								| デモ
							| 
							a.btn.btn-default(href='http://api.jquery.com/removeClass/',target='_blank')
								i.glyphicon.glyphicon-info-sign
								| &nbsp;参考
						pre(class='prettyprint linenums:1')
							:code
								doctype
								html(lang='ja-JP')
									head
										meta(charset='UTF-8')
										title
										link(rel='stylesheet',href='style.css')
									body
										p.red テキストテキストテキストテキストテキスト
										p: input(type='button',value='クリック')
										script(src='http://code.jquery.com/jquery-1.10.2.min.js')
										script(src='script.js')
						pre(class='prettyprint linenums:1')
							:code
								.red
									color #FF0000
						pre(class='prettyprint linenums:1')
							:code
								$(function() {
									$('input').click(function() {
										$('p:first-of-type').removeClass('red');
									});
								});
						pre(class='prettyprint linenums:1')
							:code
								$ ->
									$('input').click ->
										$('p:first-of-type').removeClass('red');
										return
									return