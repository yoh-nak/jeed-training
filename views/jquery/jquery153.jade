extends ../layout

block link
    include ../link1

block content
    include navbar
    .container
        .row
            .col-sm-3
                include left
            .col-sm-9
                .panel.panel-primary
                    .panel-heading
                        i.glyphicon.glyphicon-book
                        | &nbsp;a要素のdblclick()イベント課題
                    .panel-body
                        p a要素をダブルクリックして、img要素に置換し、リンク先の画像をimg要素のsrc属性にセットしてみましょう。
                        p
                            a.btn.btn-default(href='../demo/jquery/exercise156/index1.html',target='_blank')
                                i.glyphicon.glyphicon-play
                                | デモ
                            button.btn.btn-danger(type='button',data-toggle='collapse',data-target='#demo')
                                i.glyphicon.glyphicon-question-sign
                                | &nbsp;解答例
                        #demo.collapse.out
                                pre(class='prettyprint linenums:1')
                                    :code
                                        doctype
                                        html(lang='ja-JP')
                                            head
                                                meta(charset='UTF-8')
                                                title
                                            body
                                                p
                                                    a(href='img/image1.png',alt='') 画像
                                                script(src='http://code.jquery.com/jquery-1.11.0.min.js')
                                                script(src='script.js')
                                pre(class='prettyprint linenums:1')
                                    :code
                                        $(function() {
                                            $('a').dblclick(function() {
                                                $(this).replaceWith('<img src="' + $(this).attr('href') + '" alt="">');
                                            }).click(function() {
                                                return false;
                                            });
                                        });
                                pre(class='prettyprint linenums:1')
                                    :code
                                        $ ->
                                            $('a').dblclick( ->
                                                $(this).replaceWith '<img src="' + $(this).attr('href') + '" alt="">'
                                                return
                                            ).click ->
                                                return false;
                                            return