extends ../layout

block link
    include ../link1

block content
    include navbar
    .container
        .row
            .col-sm-3
                include left
            .col-sm-9
                .panel.panel-primary
                    .panel-heading
                        i.glyphicon.glyphicon-book
                        | &nbsp;リサイズ終了時にイベントを発生させる
                    .panel-body
                        p
                            a.btn.btn-default(href='../demo/jquery/exercise174/index1.html',target='_blank')
                                i.glyphicon.glyphicon-play
                                | デモ
                        pre(class='prettyprint linenums:1')
                            :code
                                doctype
                                html(lang='ja-JP')
                                    head
                                        meta(charset='UTF-8')
                                        title
                                    body
                                        ul
                                            for i in new Array(100)
                                                li テキストテキストテキストテキスト
                                        script(src='http://code.jquery.com/jquery-1.11.0.min.js')
                                        script(src='script.js')
                        pre(class='prettyprint linenums:1')
                            :code
                                $(function() {
                                    var timer;
                                    timer = false;
                                    $(window).resize(function() {
                                        if (timer !== false) {
                                            clearTimeout(timer);
                                        }
                                        timer = setTimeout(function() {
                                            return alert('リサイズ終了');
                                        }, 200);
                                    });
                                });
                        pre(class='prettyprint linenums:1')
                            :code
                                $ ->
                                    timer = false
                                    $(window).resize ->
                                        clearTimeout timer if timer isnt false
                                        timer = setTimeout(->
                                            alert('リサイズ終了')
                                        , 200)
                                        return
                                    return