extends ../layout

block link
	include ../link1

block content
	include navbar
	.container
		.row
			.col-sm-3
				include left
			.col-sm-9
				.panel.panel-primary
					.panel-heading
						i.glyphicon.glyphicon-book
						| &nbsp;css()メソッド②
					.panel-body
						p css()メソッドは、第1引数のみ指定すると、$メソッドで指定した要素の、css()メソッドの第1引数で指定したCSSプロパティを取得します。
						p
							a.btn.btn-default(href='../demo/jquery/exercise24/index1.html',target='_blank')
								i.glyphicon.glyphicon-play
								| デモ
							| 
							a.btn.btn-default(href='http://api.jquery.com/css/',target='_blank')
								i.glyphicon.glyphicon-info-sign
								| &nbsp;参考
						pre(class='prettyprint linenums:1')
							:code
								doctype
								html(lang='ja-JP')
									head
										meta(charset='UTF-8')
										title
									body
										p テキストテキストテキストテキストテキスト
										p: input(type='button',value='クリック')
										script(src='http://code.jquery.com/jquery-1.10.2.min.js')
										script(src='script.js')
						pre(class='prettyprint linenums:1')
							:code
								$(function() {
									$('input').click(function() {
										window.alert($('p:first-of-type').css('color'));
									});
								});
						pre(class='prettyprint linenums:1')
							:code
								$ ->
									$('input').click ->
										window.alert $('p:first-of-type').css 'color'
										return
									return