doctype html
html(lang='ja')
    head
        meta(charset='utf-8')
        title
        link(rel='stylesheet', href='http://code.jquery.com/mobile/1.4.2/jquery.mobile-1.4.2.min.css')
        script(src='http://code.jquery.com/jquery-1.9.1.min.js')
        script(src='http://code.jquery.com/mobile/1.4.2/jquery.mobile-1.4.2.min.js')
        script
            :js
                (function(){
                    //自動再生チェック
                    $(document).on('pageshow','#index',function(){
                        var audioObj = $('#myAudio').get(0);
                        var flag = audioObj.autoplay;
                        if (flag == true){
                            $('result').html('自動再生がONになっています');
                        }
                        else{
                            $('result').html('自動再生がOFFになっています');
                        }
                    });
                })()
    body
        #index(data-role='page')
            div(data-role='header')
                h1 自動的に再生するようにする
            div(data-role='content')
                audio#myAudio(width='192', height='48', controls='controls', autoplay='autoplay')
                    source(src='../audio1/sample.mp3')
                    source(src='../audio1/sample.ogg')
                    p お使いのブラウザではオーディオを再生できません。
                #result
            div(data-role='footer', data-position='fixed')
                h1 フッター