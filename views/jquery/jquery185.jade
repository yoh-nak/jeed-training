extends ../layout

block link
    include ../link1

block content
    include navbar
    .container
        .row
            .col-sm-3
                include left
            .col-sm-9
                .panel.panel-primary
                    .panel-heading
                        i.glyphicon.glyphicon-book
                        | &nbsp;resize()イベント
                    .panel-body
                        p
                            a.btn.btn-default(href='../demo/jquery/exercise176/index1.html',target='_blank')
                                i.glyphicon.glyphicon-play
                                | デモ
                            | 
                            a.btn.btn-default(href='http://api.jquery.com/resize/',target='_blank')
                                i.glyphicon.glyphicon-info-sign
                                | &nbsp;参考
                        hr
                        h4 高さ、幅を取得できるjQueryのメソッド
                        p ※ 要素にbox-sizingプロパティを適用した場合は誤作動することがあります。
                        pre(class='prettyprint linenums:1')
                            :code
                                //HTML文書全体の幅
                                $(document).width();

                                //HTML文書全体の高さ
                                $(document).height();

                                //HTML文書の表示画面の幅
                                $(window).width();

                                //HTML文書表示画面の高さ
                                $(window).height();

                                //CSSプロパティのwidth
                                $('セレクター').width();

                                //width()+paddingLeft+paddingRight
                                $('セレクター').innerWidth();

                                //innerWidth()+borderLeftWidth+borderRightWidth()
                                $('セレクター').outerWidth();

                                //outerWidth()+marginLeft+marginRight
                                $('セレクター').outerWidth(true);
                                
                                //CSSプロパティのheight
                                $('セレクター').height();

                                //height()+paddingTop+paddingBottom
                                $('セレクター').innerHeight();

                                //innerHeight()+borderTopWidth+borderBottomWidth()
                                $('セレクター').outerHeight();

                                //outerHeight()+marginTop+marginBottom
                                $('セレクター').outerHeight(true);
                        hr
                        pre(class='prettyprint linenums:1')
                            :code
                                doctype
                                html(lang='ja-JP')
                                    head
                                        meta(charset='UTF-8')
                                        title
                                    body
                                        ul
                                            for i in new Array(4)
                                                li テキストテキストテキストテキスト
                                        script(src='http://code.jquery.com/jquery-1.11.0.min.js')
                                        script(src='script.js')
                        pre(class='prettyprint linenums:1')
                            :code
                                $(function() {
                                    $(window).resize(function() {
                                        console.log('$(document).outerWidth(true): ' + $(document).outerWidth(true));
                                        console.log('$(document).outerWidth(): ' + $(document).outerWidth());
                                        console.log('$(document).innerWidth(): ' + $(document).innerWidth());
                                        console.log('$(document).width(): ' + $(document).width());
                                    });
                                });
                        pre(class='prettyprint linenums:1')
                            :code
                                $ ->
                                    $(window).resize ->
                                        console.log '$(document).outerWidth(true): ' +  $(document).outerWidth(true)
                                        console.log '$(document).outerWidth(): ' + $(document).outerWidth()
                                        console.log '$(document).innerWidth(): ' + $(document).innerWidth()
                                        console.log '$(document).width(): ' +  $(document).width()
                                        return
                                    return